sonar.projectKey=prakash-t91_NodeDemo-App
sonar.organization=prakash-t91


# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=NodeDemo-App
sonar.projectVersion=1.0


# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=.

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

sonar.javascript.lcov.reportPaths=./coverage/lcov.info
sonar.test.inclusions=src/**/*.spec.js, src/**/*.test.js

# Exclusions (by default node_modules, dist, etc. are excluded, but you can add more)
sonar.exclusions=**/node_modules/**,**/*.test.js,**/*.spec.js